fn main() {
    // sample line comment

    /*
    sample comment block

    /* can be nested */

    */

    // block comment can be used for disabling chunks of code
    let x = 5 + /* 90 + */ 5;
    println!("Is `x` 10 or 100? x = {}", x);
}